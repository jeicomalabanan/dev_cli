name: dev_cli
description: Dev CLI for workspace automation.
version: 1.0.0
publish_to: none

executables:
  dev: dev

environment:
  sdk: ^3.12.0

dependencies:
  path: ^1.9.0
  args: ^2.7.0
  mason: ^0.1.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
