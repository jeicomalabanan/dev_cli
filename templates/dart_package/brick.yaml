name: dart_package
description: A new brick created with the Mason CLI.
version: 0.0.1+1

environment:
  mason: ^0.1.2

vars:
  package_name:
    type: string
    description: The name of the dart package.
