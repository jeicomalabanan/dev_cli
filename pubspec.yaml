name: dev_cli
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ^3.12.0

executables:
  dev: dev_cli

dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
