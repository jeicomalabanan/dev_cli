name: monorepo
description: A new brick created with the Mason CLI.
version: 0.1.0+1

environment:
  mason: ^0.1.2

vars:
  name:
    type: string
    description: Name of the monorepo.
    prompt: What is the name of the monorepo?
